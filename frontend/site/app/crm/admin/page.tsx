import AdminPanel from "@/components/AdminPanel";

export const dynamic = "force-dynamic";
export const metadata = { title: "Настройки — GoodPeople CRM" };

export default function AdminPage() {
  return <AdminPanel />;
}
